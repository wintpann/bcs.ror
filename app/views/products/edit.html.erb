<%= content_tag :h2, "Редактировать продукт: #{@product.name}", class: 'page-title' %>
<%= render 'shared/errors' %>
<%= form_for @product, url: user_product_path do |f| %>
  <%= render 'product', f: f %>
  <%= f.submit 'Редактировать' %>
<% end %>
