<% if @all_events && @all_events.any? %>
  <% @all_events.each do |event| %>

    <% if event.event_type=='shopping'%>
      <%= link_to "Shopping: #{event.sum} rub | Date: #{to_local(event.created_at)}", url: '#' %>
    <% end %>

  <% end %>
<% end %>
