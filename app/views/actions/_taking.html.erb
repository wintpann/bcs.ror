<%= content_tag :h3, "Taking stuff from #{event.taking_events.first.employee.name}" %>
<div><%=to_local(event.created_at)%></div>
<%= render 'shared/stocks', stocks: event.taking_events %>
<div><%="Total: #{event.sum} rub"%></div>
