<h2 class="page-title">Warehouse</h2>
<% if @warehouses && @warehouses.any? %>
  <%= render 'shared/stocks', stocks: @warehouses %>
<% else %>
  <h3 class="page-title">There is no any products in warehouse</h3>
<% end %>
