<h2 class="page-title">Новая оплата налога</h2>
<%= render 'shared/errors' %>
<%= form_for :new_tax, url: user_create_tax_path do |f| %>
  <%= render 'expense', f: f, options: {description: '(не обязательно)'} %>
<% end %>
