<%= content_tag :h3, "Selling stuff: #{event.selling_events.first.employee.name}" %>
<div><%=to_local(event.created_at)%></div>
<%= render 'shared/stocks', stocks: event.selling_events %>
<div><%="Total: +#{event.sum} rub"%></div>
