<h3>All users</h3>
<div>
    <ul>
      <% @users.each do |user| %>
          <li>
            <%= link_to "#{user.name}: #{user.email}", user_path(user) %>
          </li>
      <% end %>
    </ul>
</div>
